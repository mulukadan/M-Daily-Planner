<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MDailyPlanner" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#6200EE</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- Branding image is only supported in API 31+, so we move it to a v31 specific file if needed,
             or use the compat library properly. For now, let's remove it from the main themes.xml to fix the build error. -->
        <item name="postSplashScreenTheme">@style/Theme.MDailyPlanner</item>
    </style>
</resources>
